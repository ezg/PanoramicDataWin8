<local:Renderer
    x:Class="PanoramicDataWin8.view.vis.render.RawDataRenderer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PanoramicDataWin8.view.vis.render"
    xmlns:common="using:PanoramicDataWin8.view.common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    FontFamily="{StaticResource abel}">
    <Grid  Background="{StaticResource lightBrush}">
        <common:DXSurface x:Name="dxSurface" Visibility="Collapsed"/>
        <ListView x:Name="xRawDataView" Margin="20"
                  VirtualizingStackPanel.VirtualizationMode="Recycling" 
                  SelectionMode="None"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Stretch"
                  ManipulationMode="TranslateRailsY"
                  ShowsScrollingPlaceholders="True"
                  CanDragItems="False"  >
            <ListView.Resources>
                <local:ObjectToFrameworkElementConverter x:Key="ObjectToFrameworkElementConverter"></local:ObjectToFrameworkElementConverter>
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                    <Setter Property="VerticalContentAlignment"   Value="Center" />
                    <Setter Property="Margin" Value="0,0,0,0" />
                    <Setter Property="Padding" Value="0,0,0,0" />
                    <Setter Property="MinWidth" Value="0" />
                    <Setter Property="MinHeight" Value="25" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Child="{Binding Converter={StaticResource ObjectToFrameworkElementConverter}}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</local:Renderer>
