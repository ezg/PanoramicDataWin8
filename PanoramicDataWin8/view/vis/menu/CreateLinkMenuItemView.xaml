<UserControl
    x:Class="PanoramicDataWin8.view.vis.menu.CreateLinkMenuItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PanoramicDataWin8.view.vis.menu"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    FontFamily="{StaticResource abel}">

    <Grid Width="{Binding Size.X}" Height="{Binding Size.Y}" Background="{StaticResource lightBrush}">
        <Grid Column="0" Name="backgroundGrid" Margin="4">
            <Ellipse Fill="{StaticResource backgroundBrush}"></Ellipse>
            <Ellipse Fill="{StaticResource lightBrush}" Margin="4"></Ellipse>
            <Ellipse Fill="{StaticResource backgroundBrush}" Margin="6"></Ellipse>
        </Grid>
    </Grid>
</UserControl>
    