<UserControl
    x:Class="PanoramicDataWin8.view.vis.ComparisonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PanoramicDataWin8.view.vis"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    FontFamily="{StaticResource abel}">

    <UserControl.RenderTransform>
        <TranslateTransform X="{Binding Position.X}" Y="{Binding Position.Y}"/>
    </UserControl.RenderTransform>

    <Grid Width="{Binding Size.X}" Height="{Binding Size.Y}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Canvas Grid.RowSpan="2">
            <Rectangle Name="brushRectangle"></Rectangle>
        </Canvas>
        <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" Text="p" Foreground="{StaticResource darkBrush}" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="top" Name="tbPValue"  Text="" FontSize="28" Foreground="{StaticResource darkBrush}" Margin="0,5,0,0"/>
    </Grid>
</UserControl>
